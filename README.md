# RAG-Retrieval-System
A simple semantic search system using Sentence Transformers (all-MiniLM-L6-v2). It converts documents and user queries into embeddings and retrieves the most relevant answer using similarity scoring. This project demonstrates the retrieval part of a basic RAG pipeline without LLM integration.
